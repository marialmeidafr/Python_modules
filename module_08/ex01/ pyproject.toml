[project]
name = "matrix-loading"
version = "0.1.0"
description = "ex1 matix constructer"
authors = [
    {name = "mariaalm",email = "mariaalm@student.42porto.com"}
]
requires-python = ">=3.10.12,<3.13"
dependencies = [
   
]

[tool.poetry.dependencies]
python = "^3.10"
pandas = "^2.1.0"
numpy = "^1.26.0"
matplotlib = "^3.7.2"
requests = "^2.31.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"